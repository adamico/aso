return {
   tags = "drawable",
   args = {
      x = SCREEN_WIDTH / 2 - PLAYING_AREA_WIDTH / 2,
      y = SCREEN_HEIGHT / 2 - PLAYING_AREA_HEIGHT / 2,
      width = PLAYING_AREA_WIDTH,
      height = PLAYING_AREA_HEIGHT,
      color = 5
   }
}
