return {
   tags = "animatable,controllable,drawable,player",
   args = {
      x = PLAYER_STARTING_POSITION.x,
      y = PLAYER_STARTING_POSITION.y,
      width = PLAYER_WIDTH,
      height = PLAYER_HEIGHT,
      sprite_index_offset = PLAYER_SPRITE_INDEX_OFFSET,
      animation_start = 0,
      animation_end = 1,
      animation_frame_rate = 30,
      sprite = 0,
      speed = vec(2, 2)
   }
}
